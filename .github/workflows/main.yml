name: PlatformIO CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:   # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. 安装 Python（PlatformIO 依赖）
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 3. 安装 PlatformIO Core
      - name: Install PlatformIO Core
        run: |
          python -m pip install --upgrade pip
          pip install platformio

          
      - name: Cache PlatformIO packages
        uses: actions/cache@v4
        with:
          path: ~/.platformio
          key: ${{ runner.os }}-pio-${{ hashFiles('**/platformio.ini') }}
          restore-keys: |
            ${{ runner.os }}-pio-
      
      # 新增这一行：强制重装平台，确保 framework-picosdk 被安装
      - name: Reinstall platform to ensure full dependencies
        run: pio pkg install -g -p https://github.com/blackketter/wizio-pico/archive/refs/heads/main.zip
        
      # 5. 编译项目（使用您本地的环境配置）
      - name: Build firmware
        run: pio run -t clean; pio run -v

      # （可选）上传编译产物
      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware
          path: .pio/build/pico/firmware.uf2
